# sqlite sidecars (the committed DB is share/knowledge.sqlite)
*.sqlite-journal
*.sqlite-wal
*.sqlite-shm
*.sqlite.load.sql
/build/
*~
*.swp
.DS_Store

# Harvest working trees (too large / not the git artifact)
/cache/
/corpus/collected/

# Never commit models (embeddings are computed locally, not vendored)
*.gguf
*.ggml
scripts/__pycache__/
*.pyc
